{
  "name": "@the-grove/registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:registry": "npm run build && node dist/build.js",
    "generate": "npm run build:registry",
    "validate": "npm run build && node dist/validate.js",
    "serve": "npx http-server public -p 8080 --cors"
  },
  "dependencies": {
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
