// Temporary test file - use localhost registry
const REGISTRY_BASE_URL = 'http://localhost:8080/r';

// To use:
// 1. Start registry server: cd packages/registry && npm run serve
// 2. Replace REGISTRY_BASE_URL in add.ts with the localhost URL
// 3. Build CLI: cd packages/cli && npm run build
// 4. Test: cd apps/test-app && npx the-grove add async-button
